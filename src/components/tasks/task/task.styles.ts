import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  taskBox: {
    display: 'flex',
    flexDirection: 'row',
    alignItems: 'center',
    marginVertical: 5,
  },
  taskTitle: {
    flex: 1,
    fontSize: 18,
    marginHorizontal: 10,
  },
});
